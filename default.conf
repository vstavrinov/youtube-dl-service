server {
    listen 0.0.0.0:PORT;
    location / {
        gzip off;
        fastcgi_param  SCRIPT_FILENAME /srv/ydl;
        include fastcgi.conf;
        fastcgi_pass unix:/srv/fcgiwrap.socket;
        fastcgi_buffering off;
    }
}
# vim: filetype=nginx
